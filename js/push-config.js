// Fill these in after deploying push-relay/ (see push-relay/README.md).
// Background alerts stay unavailable — the Settings toggle shows as such —
// until both are set.
export const RELAY_URL = '';
export const VAPID_PUBLIC_KEY = '';
